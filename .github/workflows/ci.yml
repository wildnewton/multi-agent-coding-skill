name: CI

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    name: Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version:
          - "3.10"
          - "3.13"

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Run targeted script tests
        run: >-
          python -m unittest
          tests/test_run_codex.py
          tests/test_git_ownership.py
          -v

      - name: Run full test suite
        run: python -m unittest discover -s tests -v

      - name: Compile runtime script
        run: python -m compileall -q run_codex.py
